services:
  rpi-seism-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rpi-seism-web
    ports:
      - 7000:80
    environment:
      - UID=1000
      - GID=1000
    restart: unless-stopped
